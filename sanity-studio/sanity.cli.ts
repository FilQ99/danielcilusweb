// sanity-studio/sanity.cli.ts

import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'lla8sj8e',
    dataset: 'production'
  }
  // Blok 'deployment' został usunięty stąd
})
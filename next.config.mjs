// Poprawna wersja w next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tutaj możesz dodać konfigurację w przyszłości, np. dla obrazków
  // images: {
  //   remotePatterns: [{ hostname: 'cdn.sanity.io' }],
  // },
};

export default nextConfig;